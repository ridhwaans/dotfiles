# Enable mouse control (clickable windows, panes, resizable panes)
set -g mouse on

# Disable automatic window rename to conserve CPU
set-option -g allow-rename off

# Vim-like movement
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# Start window numbering at 1
set -g base-index 1

# Remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Split panes using | and -
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Reload tmux config
bind r source-file ~/.tmux.conf

# Plugins
set -g @plugins 'tmux-plugins/tpm'

run -b '~/.tmux/plugins/tpm/tpm'

# Theme
set -g @theme_dir "$HOME/Source/environment/devcontainer-features/src/base/themes"

set -g @theme_name "gotham"

set -g @theme_file "#{@theme_dir}/#{@theme_name}/theme.conf"

run-shell "test -f '#{@theme_file}' && tmux source-file '#{@theme_file}' || tmux display-message \"Theme file '#{@theme_file}' not found!\""
